<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="/css/main.css" />
    <title>Course Evaluation</title>
  </head>
  <body>
    <div>
      <header>
        <a id="SchoolPage" href="https://www.centennialcollege.ca/"
          >Continuing Education Course/Instructor Evaluation</a
        >
      </header>
      <hr />
      <!-- Display flash messages  -->
      <% for(var i in messages) { %>
      <div class="flash"><%= messages[i] %></div>
      <% } %>

      <!-- If a user is authenticated, display the user's email , otherwise display the authentication links -->
      <% if ( userEmail ) { %>
      <div class="form-signin">
        <h1 class="h3 mb-3 font-weight-normal">Hi <%=userEmail%></h1>
        <br />
        <a class="btn btn-lg btn-primary btn-block" href="/logout">Sign out</a>
        <a class="btn btn-lg btn-primary btn-block" href="/submitcomments"
          >Submit New Comments</a
        >
        <a class="btn btn-lg btn-info btn-block" href="/display"
          >Display All Students</a
        >
        <a class="btn btn-lg btn-info btn-block" href="/comments"
          >Display all Comments</a
        >
      </div>
      <% } else { %>
      <form method="post" class="form-signin">
        <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
        <label for="username" class="sr-only">Email address</label>
        <input
          type="email"
          name="username"
          class="form-control"
          placeholder="Email address"
          required
          autofocus
        />
        <label for="password" class="sr-only">Password</label>
        <input
          type="password"
          size="30"
          name="password"
          class="form-control"
          placeholder="Password"
          required
        />
        <button class="btn btn-lg btn-primary btn-block" type="submit">
          Sign In
        </button>
        <a class="btn btn-lg btn-primary btn-block" href="/signup"
          >Sign Up as Student</a
        >
        <a class="btn btn-lg btn-info btn-block" href="/display"
          >Display All Students</a
        >
        <a class="btn btn-lg btn-info btn-block" href="/comments"
          >Display all Comments</a
        >
      </form>
      <% } %>
    </div>
    <p class="mt-5 mb-3 text-muted text-center">
      © Hang Li - 300993981 - COMP 308 - Lab 2
    </p>
  </body>
</html>
